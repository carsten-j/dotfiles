
R_LIBS_USER=~/Projects/R_libs/ 

print("This is the Rprofile inside the foo project!")


## Don't ask me for my CRAN mirror every time
options("repos" = c(CRAN = "http://cran.rstudio.com/"))

print("This is the Rprofile inside the foo project!")

options(prompt="ℝ> ")

## .First() run at the start of every R session. 
## Use to load commonly used packages? 
.First <- function() {
  library(readr)
 
  ## Create a new invisible environment for all the functions to go in so it doesn't clutter your workspace.
  foo <- new.env()

  ## Open Finder to the current directory on mac
  foo$macopen <- function(...) if(Sys.info()[1]=="Darwin") system("open .")
  foo$o       <- function(...) if(Sys.info()[1]=="Darwin") system("open .")

  ## Attach all the variables above
  base::attach(foo, name = "foo", warn.conflicts = FALSE)

  cat("\nSuccessfully loaded .Rprofile at", date(), "\n")
}

## .Last() run at the end of the session
.Last <- function() {
  # save command history here?
  cat("\nGoodbye at ", date(), "\n")
}

print("This is the Rprofile inside the foo project!")


